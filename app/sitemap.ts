import type { MetadataRoute } from "next";import { articles } from "./lib/articles";
export default function sitemap():MetadataRoute.Sitemap{const base="https://myrarescore.com";const staticPages=["","/tests","/are-you-rare","/iq-test","/morality-test","/blog","/press","/share","/faq","/contact","/privacy","/terms"];const blogPages=articles.map((a)=>`/blog/${a.slug}`);return[...staticPages,...blogPages].map((path)=>({url:`${base}${path}`,lastModified:new Date(),changeFrequency:path.startsWith("/blog")?"weekly":"monthly",priority:path===""?1:path.startsWith("/blog/")?.78:.8}))}
